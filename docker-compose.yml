version: '3.8'
services:
  crucix:
    build: .
    ports:
      - "${PORT:-3117}:${PORT:-3117}"
    env_file:
      - .env
    environment:
      - DOCKER=1
    volumes:
      - ./runs:/app/runs
    restart: unless-stopped
